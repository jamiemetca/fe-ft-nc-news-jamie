import React from 'react'

const LoginSignin = () => {
  return <div>
    <div>This will be a loginBox/Signin box</div>
    <div>Each will be conditionally rendered depending on the link clicked.</div>
  </div>
}

export default LoginSignin;